% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpretation.R
\name{run_interpretation}
\alias{run_interpretation}
\title{Run Interpretation (Workflow + Executor)}
\usage{
run_interpretation(interpretation_object, interpreter = NULL, ...)
}
\arguments{
\item{interpretation_object}{An \code{interpretation_object}, as returned
by \code{\link[=create_interpretation_object]{create_interpretation_object()}}.}

\item{interpreter}{A function taking \verb{(interpretation_object, ...)}
and returning a named list of interpretation components. Defaults
to an internal generic, structural placeholder interpreter.}

\item{...}{Additional arguments forwarded to \code{interpreter}.}
}
\value{
A list with elements \code{interpretation_object} (the updated
object, with \code{interpretation_components} populated) and
\code{interpretation_evidence} (the resulting Framework artifact).
}
\description{
Orchestrates the Interpretation workflow for an initial
Interpretation Object: invokes an interpreter (the default generic
interpreter, or a caller-supplied one) to produce one or more named
\code{interpretation_components}, returns an updated Interpretation
Object carrying those components, and constructs the resulting
Interpretation Evidence -- mirroring Module 09's \code{run_validation()},
Module 10's \code{run_benchmark()}, and Module 11's \code{run_candidate_selection()}.
}
\details{
The interpreter replaces only the execution step; it never bypasses
workflow, verification, lineage, or artifact construction performed
by this function. The default interpreter performs only a generic,
structural check -- no pathway analysis, enrichment analysis,
explainability algorithms (SHAP, feature importance), or biological
reasoning is performed by this function or its default interpreter.
}
