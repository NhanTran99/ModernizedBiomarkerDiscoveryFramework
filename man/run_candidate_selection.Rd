% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/candidate_selection.R
\name{run_candidate_selection}
\alias{run_candidate_selection}
\title{Run Candidate Selection (Workflow + Executor)}
\usage{
run_candidate_selection(candidate_selection_object, selector = NULL, ...)
}
\arguments{
\item{candidate_selection_object}{A \code{candidate_selection_object}, as
returned by \code{\link[=create_candidate_selection_object]{create_candidate_selection_object()}}.}

\item{selector}{A function taking \verb{(candidate_selection_object, ...)}
and returning a named list of selection components. Defaults to
an internal generic, structural placeholder selector.}

\item{...}{Additional arguments forwarded to \code{selector}.}
}
\value{
A list with elements \code{candidate_selection_object} (the
updated object, with \code{selection_components} populated) and
\code{validated_candidate} (the resulting, final Framework artifact of
IDS-004).
}
\description{
Orchestrates the Candidate Selection workflow for an initial
Candidate Selection Object: invokes a selector (the default generic
selector, or a caller-supplied one) to produce one or more named
\code{selection_components}, returns an updated Candidate Selection
Object carrying those components, and constructs the resulting
Validated Candidate -- mirroring Module 09's \code{run_validation()} and
Module 10's \code{run_benchmark()}.
}
\details{
The selector replaces only the execution step; it never bypasses
workflow, verification, lineage, or artifact construction performed
by this function. The default selector performs only a generic,
structural check -- no ranking, score optimization, statistical
comparison, biological interpretation, clinical recommendation, or
machine learning methodology is performed by this function or its
default selector.
}
