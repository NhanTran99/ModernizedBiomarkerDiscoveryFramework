% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/candidate_selection.R
\name{create_validated_candidate}
\alias{create_validated_candidate}
\title{Create a Validated Candidate}
\usage{
create_validated_candidate(
  candidate_selection_object,
  producer = NULL,
  consumer = "interpretability",
  metadata = list()
)
}
\arguments{
\item{candidate_selection_object}{A \code{candidate_selection_object},
typically the updated object produced by \code{\link[=run_candidate_selection]{run_candidate_selection()}}.}

\item{producer}{Character scalar identifying the producer. Defaults
to the embedded Benchmark Evidence's \code{producer}.}

\item{consumer}{Character scalar identifying the intended downstream
consumer. Defaults to \code{"interpretability"} (Phase E, Modules 12-13).}

\item{metadata}{Additional list of metadata merged into the
artifact's \code{metadata} field. Defaults to an empty list. Must not
contain \code{framework_layer}, \code{artifact_role}, or
\code{candidate_selection_object} (set internally), nor
\code{scientific_interpretation}, \code{biological_recommendation},
\code{clinical_recommendation}, \code{publication_ready_conclusions}, or
\code{therapeutic_recommendations} (explicitly forbidden content).}
}
\value{
An object of class \code{c("validated_candidate", "artifact_contract", "contract")}.
}
\description{
Constructs Validated Candidate: the sole Framework artifact produced
by Module 11 (via the LOCKED Module 02 \code{create_artifact_contract()}),
and the final Framework artifact produced by IDS-004. Structurally
independent from the supplied Candidate Selection Object while
preserving complete lineage by embedding it unchanged. Validated
Candidate never contains scientific interpretation, biological
recommendation, clinical recommendation, publication-ready
conclusions, or therapeutic recommendations.
}
